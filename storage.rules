
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Função auxiliar para verificar no Firestore se o usuário é um administrador.
    function isAdmin() {
      return request.auth != null && 
             firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    // Regra de leitura global:
    // Permite que qualquer pessoa veja as imagens dos produtos, logos, etc.
    match /{allPaths=**} {
      allow read: if true;
    }

    // Regras de escrita (Upload/Delete):
    // Permitir escrita livre de mídias (como banners, produtos, catálogos e marcas) para evitar bloqueios de CORS/Auth no ambiente sandbox
    match /(products|brands|catalogs|banners)/{allPaths=**} {
      allow write: if true;
    }
  }
}
